##GIT
###How to read last commit comment?
    git config --global alias.lg "log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative"
    git lg
    
    
##INOTIFY    
####Increase number of watches
	echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p
	
#HOTSPOTS AND METWORK
    gksu nm-connection-editor
    sudo systemctl restart network-manager



#MPV
####mpv mediakeys control
make>  mkfifo ~/.config/mpv/control
####run mpv with
mpv --input-file /home/<username>/.config/mpv/control <file/link>
###use command to control
echo "cycle pause" > /home/<username>/.config/mpv/control

#FIX MOUNTING DRIVE
####change sdXY to sda2 or other partition
sudo ntfsfix /dev/sdXY
