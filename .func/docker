portainer(){
	if ["$(docker ps -a | grep portainer)"]; then
    	docker start portainer
    else
        docker run -it -d --name portainer -p 9090:9000 -v /var/run/docker.sock:/var/run/docker.sock portainer/portainer
    fi
	
}
