NAME=bibliography.bib

doi_bib(){
    echo >> $NAME
    curl -s "http://api.crossref.org/works/$1/transform/application/x-bibtex" >> $NAME
    echo >> $NAME
}

get_doi(){
    pdftotext $1 - | grep -i "doi"
}
